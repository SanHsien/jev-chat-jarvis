# 更新日誌

中文 | [English](CHANGELOG.en.md)

格式依 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)。版本號為 `上游主.次.fork 序號`：
`1.4.0` 表示基於上游 1.4 的第 0 版，之後的 fork 版本為 `1.4.1`、`1.4.2`…。
上游 1.0–1.4 的歷史見上游 repo 的 `CHANGELOG.md`。

## [未發佈]

## [1.4.11] - 2026-09-26

### 變更
- **懸浮窗不再自己展開**：捲動 LINE 對話時不會再跳出訊息畫面。點懸浮球才展開，當下解析分析對象並顯示「分析」「略過」，按「分析」才呼叫模型。
- **Windows 版改為半自動**（同 Android）：對方有新訊息時只顯示分析對象與「分析」按鈕，按了才呼叫模型；設定頁新增「對方發訊息時自動分析」（預設關）。半自動時換回覆對象不會自動重跑，除非該會話已經分析過。
- repo 歷史改為單一根 commit（不再顯示上游的 143 個 commit）；分歧檢查工具需要時會自動抓上游基準。
- 分診 Android 上游新 PR #64（綁定來源會話，已由本 fork 1.4.5 自行修正，暫緩改用上游版本）。

## [1.4.10] - 2026-09-25

### 新增
- **Windows 版正式隨 Release 發佈**：`chat-wingman-windows-v1.4.10.zip`（與 APK 同版號），基於 jev-chat-windows 二次開發、只支援 LINE 電腦版；LINE 版面尚未實機驗證，「填入」暫時只複製到剪貼簿。
- Windows 版啟動時檢查更新改查本 repo 的 Release（可在設定關閉）。
- Windows 版：支援 SOCKS 代理（採上游 PR #15）；滑鼠主鍵對調（左撇子）時填入點擊正確（上游 issue #34）。
- Windows 版依上游〈版權與許可〉：設定頁新增〈關於與授權〉，README 與 Release 說明寫明「基於 JevChat-Windows 二次開發」、GPLv3 元件說明，zip 保留上游 LICENSE／NOTICE；README 新增致謝與更完整的免責聲明。
- Windows 上游的分支、PR、issue 已逐筆評估並記錄在 `docs/UPSTREAM.md`。

### 變更
- Windows 版不再是 repo 裡的獨立子專案：程式碼成為 `wingman/` 套件（`python -m wingman`），依賴、打包、授權、忽略清單與 CI 都併入 repo 根目錄；圖示改用新 logo。
- README（中英）整合 Windows 版的出處、安裝、設定、使用方式、運作方式、已知限制與授權；PRIVACY 新增 Windows 版章節。

## [1.4.9] - 2026-09-25

### 新增
- **Windows 版預覽**：jev-chat-windows 拆進本 repo（`wingman/` 套件），改為只認 LINE 電腦版；起草預設 OpenRouter + `google/gemini-2.5-flash`；介面繁體中文。LINE 版面尚未實機驗證，「填入」暫時只複製到剪貼簿。GitHub Actions 的 `windows` workflow 會打包 exe（artifact）。

### 變更
- **預設改為半自動分析**：偵測到對方新訊息時，懸浮窗顯示分析對象與最新訊息，按「分析」才送出（「略過」收起）。貼圖、照片或不需要回的訊息不再自動花 token。升級時會把「對方發訊息時自動分析」關掉一次，之後可在設定重新打開。
- 產品名改為不限 LINE 的「對話副駕」（英文 Chat Wingman）。套件名不變，可直接覆蓋升級。

## [1.4.8] - 2026-09-25

### 變更
- 產品改名為「LINE 對話副駕」：App 名稱、首頁標題、常駐通知與 README 都用新名稱，不再用上游的「Jev 聊天助手」當產品名（出處聲明照舊）。套件名不變，可直接覆蓋升級。

## [1.4.7] - 2026-09-25

### 移除
- 精簡接口預設：判斷接口保留 OpenRouter、TypeSafe 直連、Vercel、OpenCode Zen、自定義；回覆與視覺接口保留 OpenRouter、Vercel、自定義（移除判斷的博查 Jev、回覆的 DeepSeek 官方與通義相容、視覺的通義相容）。
- 回覆與視覺的預設模型改為 `google/gemini-2.5-flash`（OpenRouter、Vercel 皆同）。
- 升級時一次性遷移舊設定：接口仍指向已移除預設的，改為 Vercel AI Gateway 並清掉該接口的金鑰（要重新填 Vercel 金鑰）；在 OpenRouter／Vercel 上選了 DeepSeek／Qwen 模型的，換成該接口的預設模型。自訂接口上的模型不動。

### 新增
- 設定頁「關於與隱私」註明「基於 Jev 聊天助手二次開發」與授權，新增「上游專案」與「授權條款與聲明」；APK 內附 `LICENSE` 與 `NOTICE`。Release 說明也附上出處。

### 變更
- 新 logo 與 App 圖示；README 移除與本 fork 不符的上游截圖。

## [1.4.6] - 2026-09-25

### 變更
- App 介面文字與 `PRIVACY.md` 全部改為繁體中文（台灣用語）；候選回覆與摘要也要求用繁體中文輸出。

### 新增
- Android 13 以後，非商店安裝的 App 開無障礙／懸浮窗會被「受限制的設定」擋下：首頁在權限未開時顯示引導，並提供一鍵開啟「應用程式資訊」，照著點 ⋮ →「允許受限制的設定」即可。

## [1.4.5] - 2026-09-25

### 修正（上游 issue #61）
- 分析結果只顯示在發起分析的那個對話；切換對話後舊結果不再出現在新對話裡，點候選也不會填進別的聊天室（改為提示重新分析）。
- 分析進行中收到的新訊息，會在本次結束後自動補分析，不再被丟掉。
- 新對話載入中（標題暫時是「連線中」之類）時，不再借用上一個聯絡人的名字，避免聊天歷史寫進別人的紀錄。

## [1.4.4] - 2026-09-25

### 新增
- **群組聊天分辨發言者（本 fork 原創）**：LINE 從頭像的「某某的個人圖片」取得發言者，同一人連發的訊息沿用；X 群組私訊用發件人名稱。
  分析對象改為「對方最新一則訊息的發言者」，其他人的發言只當背景；Jev 判斷帶入發言者與分析對象，候選回覆以該人為對象，面板顯示分析對象。
  一對一聊天的判斷內容與先前完全相同。

## [1.4.3] - 2026-09-25

### 新增
- **LINE 自動讀取（本 fork 原創）**：`LineAdapter` 依 2026-09-25 的真機 dump 完成並啟用。讀取會話標題、文字訊息，照片與貼圖以「[照片]」「[貼圖]」帶入；「我／對方」依 LINE 訊息列的已讀／時間標記判斷。附 JVM 單元測試。

## [1.4.2] - 2026-09-24

### 修正
- 恢復微信封鎖：在微信裡不顯示懸浮球、不讀取、不截圖。上游使用者回報截取微信畫面會讓微信對該裝置開啟防截圖（上游 issue #42、#46、#47、#52）；1.4.1 取消封鎖是錯誤決定。

## [1.4.1] - 2026-09-24

### 修正（採用上游未合併的 PR）
- 切換聯絡人時不再沿用上一個會話的分析：會話去重納入標題（上游 #6）。
- 候選回覆開頭的數字（例如「1.5 小時」）不再被誤刪（上游 #21）。
- 無障礙服務斷線後，首頁改顯示「讀屏服務未連接」而不是「已就緒」；背景保活失敗會提示（上游 #35）。
- 簽章設定只在指定 `JEV_KEYSTORE_PROPS` 時啟用，Linux／macOS 建置不再因 Windows 路徑失敗（上游 #32）。

### 變更
- 移除 QQ 支援與微信封鎖清單；README 加上「勿在微信裡用手動截屏識別」的警告。

## [1.4.0] - 2026-09-24

基於上游 `jev-chat/jev-chat-jarvis` 1.4（`a3026e2`）。

### 新增
- **LINE 支援（本 fork 原創）**：手動模式可用（懸浮窗選單「截屏識別一次」）；`LineAdapter` 自動模式骨架已加入，等真機驗證後啟用。
- 回覆與視覺接口新增「Vercel」預設；全新安裝三路接口一次性預設為 Vercel AI Gateway。
- GitHub Releases 附 APK 與 `.sha256`（`release` workflow）；CI 每次建置也上傳 debug APK。

### 變更
- 建置工具鏈升級：AGP 9.4.1、Gradle 9.7.1（改用 AGP 內建 Kotlin）、AndroidX appcompat 1.8.0、material 1.14.0、constraintlayout 2.2.2。
- 無障礙服務改用本 App 自己的類別名（`com.jev.probe.capture.ChatCaptureService`），不再偽裝成系統元件；**從舊版升級後需要重新開啟無障礙**。
- 隱私權政策與 App 內連結改指向本 fork。

### 移除
- 微信、飛書與 QQ 的適配器、相關程式碼與說明。
- 上游預先打包的 APK、上游官網原始碼（`site/`）、上游內部規劃文件與簡體中文 README。
